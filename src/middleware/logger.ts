/**
 * Deprecated legacy middleware helper. The structured logging logic now lives in `src/middleware.ts`.
 * This file remains as a harmless stub to prevent broken imports until we confirm nothing references it.
 */

export {};
